import React, { useState } from "react";

const Button = ({onClick}) => {
    
    return(
         
        <button onClick={onClick}>
        "Show"
        </button>
       
    
    )
}

export default Button